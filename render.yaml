services:
  - type: web
    name: zoom-clone
    runtime: node
    plan: free

    buildCommand: bash build.sh
    startCommand: npm start
    healthCheckPath: /api/test

    envVars:
      - key: NODE_ENV
        value: production
